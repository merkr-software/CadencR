[package]
name = "cli-discovery"
version = "0.6.1"
edition = "2021"
license.workspace = true

[dependencies]
futures = "0.3"
once_cell = "1"
regex-lite = "0.1"
tokio = { version = "1", features = ["process", "sync", "time", "rt", "macros", "io-util"] }
tracing = "0.1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
