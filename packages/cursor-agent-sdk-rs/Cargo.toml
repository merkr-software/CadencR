[package]
name = "cursor-agent-sdk-rs"
version = "0.10.0"
edition = "2021"
license.workspace = true

[dependencies]
bon.workspace = true
cli-discovery = { path = "../cli-discovery" }
once_cell = "1"
thiserror = "2"
tokio = { version = "1", features = ["process", "sync", "time"] }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
