{
  "name": "@cadencr/opencode-sdk-rs",
  "private": true,
  "version": "0.10.0",
  "description": "Rust SDK wrapping the OpenCode CLI — used by Cadencr to stream and control OpenCode sessions.",
  "license": "Apache-2.0",
  "scripts": {
    "format": "cargo fmt",
    "format:check": "cargo fmt -- --check"
  }
}
